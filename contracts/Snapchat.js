pragma solidity ^0.4.4;

contract Snapchat {
  address public owner;
  uint public last_completed_migration;

  function send(address recipient, string photo) {
    // send photo to address
  }

  function list() {
    // list photos intended me
  }
}
